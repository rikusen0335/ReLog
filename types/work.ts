type WorkType = {
    slug: string
    title: string
    subtitle?: string
    introduction: string
    description: string
    images: string[]
    thumbnail?: string
    used: string[]
}

export default WorkType
